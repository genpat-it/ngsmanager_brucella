process {
  stageInMode='rellink'
}

params {
  max_retries = 2
  always_retry = true 
  raw_reads_threshold = 100
  error_strategy_after_retries = 'ignore'
  incompatible_step_warning = false
  max_time = '12h'
}